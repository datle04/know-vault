# API
NEXT_PUBLIC_API_URL=http://localhost:3001

# Auth (populated by backend login response, stored in memory)
# No env vars needed for auth tokens — they live in Zustand + httpOnly cookies

# Optional: Sentry (Phase 6)
# NEXT_PUBLIC_SENTRY_DSN=
